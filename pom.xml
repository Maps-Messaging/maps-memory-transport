<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.mapsmessaging</groupId>
  <artifactId>memory-transport</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <name>Maps Memory Transport</name>
  <description>High-performance shared-memory and RDMA transports for Java</description>
  <url>https://github.com/Maps-Messaging/maps-memory-transport</url>

  <licenses>
    <license>
      <name>Apache License, Version 2.0 with Commons Clause</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <properties>
    <dependency.check.autoUpdate>true</dependency.check.autoUpdate>
    <dependency.check.dataDirectory>${user.home}/.m2/repository/org/owasp/dependency-check-data/</dependency.check.dataDirectory>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>
    <junit.version>5.13.4</junit.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.5</version>
        <configuration>
          <useModulePath>false</useModulePath>
        </configuration>
      </plugin>
          <plugin>
        <groupId>org.owasp</groupId>
        <artifactId>dependency-check-maven</artifactId>
        <version>13.0.0</version>
        <configuration>
          <autoUpdate>${dependency.check.autoUpdate}</autoUpdate>
          <dataDirectory>${dependency.check.dataDirectory}</dataDirectory>
          <failBuildOnCVSS>10</failBuildOnCVSS>
          <failOnError>false</failOnError>
          <skipArtifactType>test-jar</skipArtifactType>
          <skipTestScope>true</skipTestScope>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
</plugins>
  </build>
  <profiles>
    <profile>
      <id>buildkite-dependency-check-cache</id>
      <activation>
        <property>
          <name>env.BUILDKITE</name>
          <value>true</value>
        </property>
      </activation>
      <properties>
        <dependency.check.autoUpdate>false</dependency.check.autoUpdate>
        <dependency.check.dataDirectory>/var/cache/dependency-check/13</dependency.check.dataDirectory>
      </properties>
    </profile>
  </profiles>

</project>
